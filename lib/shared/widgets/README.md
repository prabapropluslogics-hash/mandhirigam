# Shared widgets

Cross-feature widgets that are **not** part of the visual design system
(e.g. specialized composers that combine multiple design-system pieces for a
shared product flow).

Prefer `design_system/components` for buttons, inputs, spacing, feedback, and
other primitive UI.
